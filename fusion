{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceType":"datasetVersion","sourceId":9057601},{"sourceType":"datasetVersion","sourceId":18849201},{"sourceType":"datasetVersion","sourceId":18849219},{"sourceType":"datasetVersion","sourceId":18849239},{"sourceType":"datasetVersion","sourceId":19129332},{"sourceType":"datasetVersion","sourceId":19129623},{"sourceType":"datasetVersion","sourceId":19387175},{"sourceType":"datasetVersion","sourceId":19732119}],"dockerImageVersionId":31259,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import os\nimport shutil\nimport glob\n\n# 1. กำหนดโฟลเดอร์ต้นทางและปลายทาง\nDIR1 = '/kaggle/input/datasets/switchonchannel/9datav1'\nDIR2 = '/kaggle/input/datasets/switchonchannel/bakanae/bakanae.v1i.yolo26'\nOUT_DIR = '/kaggle/working/10dataset_v2'\n\n# สร้างโฟลเดอร์เปล่าๆ เตรียมไว้ (train, valid, test)\nsplits = ['train', 'valid', 'test']\nfor split in splits:\n    os.makedirs(os.path.join(OUT_DIR, split, 'images'), exist_ok=True)\n    os.makedirs(os.path.join(OUT_DIR, split, 'labels'), exist_ok=True)\n\ndef process_and_combine():\n    for split in splits:\n        print(f\"กำลังดำเนินการโฟลเดอร์: {split} ...\")\n        \n        # ---------------------------------------------------------\n        # ส่วนที่ 1: ดึงข้อมูลจาก 4dicease (ก๊อปปี้มาเลย ไม่ต้องแก้ Label)\n        # ---------------------------------------------------------\n        d1_img_dir = os.path.join(DIR1, split, 'images')\n        d1_lbl_dir = os.path.join(DIR1, split, 'labels')\n        \n        if os.path.exists(d1_img_dir):\n            for img_path in glob.glob(os.path.join(d1_img_dir, '*.*')):\n                filename = os.path.basename(img_path)\n                # ใช้ชื่อไฟล์เดิม ไม่มีการเติม prefix\n                shutil.copy(img_path, os.path.join(OUT_DIR, split, 'images', filename))\n                \n                txt_name = os.path.splitext(filename)[0] + '.txt'\n                lbl_path = os.path.join(d1_lbl_dir, txt_name)\n                if os.path.exists(lbl_path):\n                    shutil.copy(lbl_path, os.path.join(OUT_DIR, split, 'labels', txt_name))\n\n        # ---------------------------------------------------------\n        # ส่วนที่ 2: ดึงข้อมูลจาก falsesmuts (แปลง Label จาก 0 เป็น 4)\n        # ---------------------------------------------------------\n        d2_img_dir = os.path.join(DIR2, split, 'images')\n        d2_lbl_dir = os.path.join(DIR2, split, 'labels')\n        \n        if os.path.exists(d2_img_dir):\n            for img_path in glob.glob(os.path.join(d2_img_dir, '*.*')):\n                filename = os.path.basename(img_path)\n                # ใช้ชื่อไฟล์เดิม ไม่มีการเติม prefix\n                shutil.copy(img_path, os.path.join(OUT_DIR, split, 'images', filename))\n                \n                txt_name = os.path.splitext(filename)[0] + '.txt'\n                lbl_path = os.path.join(d2_lbl_dir, txt_name)\n                new_lbl_path = os.path.join(OUT_DIR, split, 'labels', txt_name)\n                \n                if os.path.exists(lbl_path):\n                    with open(lbl_path, 'r') as f:\n                        lines = f.readlines()\n                    \n                    new_lines = []\n                    for line in lines:\n                        parts = line.strip().split()\n                        # กรองเอาเฉพาะคลาส 0 แล้วเปลี่ยนเป็น 5\n                        if parts and int(parts[0]) == 0:\n                            parts[0] = '9'\n                            new_lines.append(' '.join(parts) + '\\n')\n                    \n                    with open(new_lbl_path, 'w') as f:\n                        f.writelines(new_lines)\n                else:\n                    # ถ้าไม่มีไฟล์ Label เดิม ให้สร้างไฟล์เปล่าๆ ไว้\n                    open(new_lbl_path, 'w').close()\n\nprocess_and_combine()\n\n# ---------------------------------------------------------\n# ส่วนที่ 3: สร้างไฟล์ data.yaml ใหม่\n# ---------------------------------------------------------\nyaml_content = f\"\"\"path: {OUT_DIR}\ntrain: train/images\nval: valid/images\ntest: test/images\n\nnc: 10\nnames:\n  0: Bacterial_Blight\n  1: Brown_Spot\n  2: Rice_Blast\n  3: Narrow_Brown_Spot\n  4: False_Smut\n  5: Dirty_Seed\n  6: Sheath_Rot\n  7: Stem_Rot\n  8: Red_Stripe\n  9: Bakanae\n\"\"\"\n\nwith open(os.path.join(OUT_DIR, 'data.yaml'), 'w') as f:\n    f.write(yaml_content)\n\nprint(\"\\n✅ รวม Dataset เสร็จสมบูรณ์! พร้อมนำไปเทรนแล้วครับ\")\nprint(f\"ไฟล์ทั้งหมดอยู่ที่: {OUT_DIR}\")","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-05-24T13:29:38.670667Z","iopub.execute_input":"2026-05-24T13:29:38.671612Z","iopub.status.idle":"2026-05-24T13:29:46.908947Z","shell.execute_reply.started":"2026-05-24T13:29:38.67157Z","shell.execute_reply":"2026-05-24T13:29:46.907994Z"}},"outputs":[{"name":"stdout","text":"กำลังดำเนินการโฟลเดอร์: train ...\nกำลังดำเนินการโฟลเดอร์: valid ...\nกำลังดำเนินการโฟลเดอร์: test ...\n\n✅ รวม Dataset เสร็จสมบูรณ์! พร้อมนำไปเทรนแล้วครับ\nไฟล์ทั้งหมดอยู่ที่: /kaggle/working/10dataset_v2\n","output_type":"stream"}],"execution_count":4},{"cell_type":"code","source":"import shutil\nimport os\n\n# โฟลเดอร์ที่เราเพิ่งสร้างเสร็จ (จากขั้นตอนที่แล้ว)\nsource_dir = '/kaggle/working/7dataset_cleaned'\n\n# ชื่อไฟล์ Zip ที่จะส่งออกมา (ไม่ต้องเติม .zip นะครับ โค้ดจะเติมให้เอง)\noutput_filename = '/kaggle/working/7dataset_v2(80%)'\n\nprint(\"กำลังบีบอัดไฟล์... กรุณารอสักครู่\")\n\n# คำสั่งสร้างไฟล์ Zip\nshutil.make_archive(output_filename, 'zip', source_dir)\n\nprint(f\"✅ เรียบร้อย! ไฟล์ Zip พร้อมให้ดาวน์โหลดแล้วที่: {output_filename}.zip\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-05-26T01:17:05.641795Z","iopub.execute_input":"2026-05-26T01:17:05.642503Z","iopub.status.idle":"2026-05-26T01:17:12.04325Z","shell.execute_reply.started":"2026-05-26T01:17:05.642471Z","shell.execute_reply":"2026-05-26T01:17:12.042324Z"}},"outputs":[{"name":"stdout","text":"กำลังบีบอัดไฟล์... กรุณารอสักครู่\n✅ เรียบร้อย! ไฟล์ Zip พร้อมให้ดาวน์โหลดแล้วที่: /kaggle/working/7dataset_v2(80%).zip\n","output_type":"stream"}],"execution_count":4},{"cell_type":"code","source":"import os\nimport glob\nfrom collections import Counter\n\n# 1. ระบุโฟลเดอร์ Dataset ปลายทางของคุณ (อิงจากในภาพ)\nDATASET_PATH = '/kaggle/working/10dataset_v2'\n\n# 2. ระบุชื่อคลาสให้ตรงกับ data.yaml\nclass_names = {\n    0: 'Bacterial_Blight',\n    1: 'Brown_Spot',\n    2: 'Rice_Blast',\n    3: 'Narrow_Brown_Spot',\n    4: 'False_Smut',\n    5: 'Dirty_Seed',\n    6: 'Sheath_Rot',\n    7: 'Stem_Rot',\n    8: 'Red_Stripe',\n    9: 'Bakanae'\n}\n\nsplits = ['train', 'valid', 'test']\ntotal_counts = Counter()\n\nprint(\"📊 กำลังตรวจสอบจำนวนข้อมูลแต่ละคลาส...\\n\")\n\nfor split in splits:\n    label_dir = os.path.join(DATASET_PATH, split, 'labels')\n    split_counts = Counter()\n    \n    # ตรวจสอบว่ามีโฟลเดอร์ labels หรือไม่\n    if os.path.exists(label_dir):\n        # อ่านไฟล์ .txt ทุกไฟล์ในโฟลเดอร์\n        for txt_file in glob.glob(os.path.join(label_dir, '*.txt')):\n            with open(txt_file, 'r') as f:\n                lines = f.readlines()\n                for line in lines:\n                    parts = line.strip().split()\n                    if parts: # ถ้าบรรทัดไม่ว่าง\n                        try:\n                            class_id = int(parts[0])\n                            split_counts[class_id] += 1\n                            total_counts[class_id] += 1\n                        except ValueError:\n                            pass\n                            \n    # แสดงผลของแต่ละโฟลเดอร์ (train, valid, test)\n    print(f\"📁 โฟลเดอร์: {split.upper()}\")\n    for cls_id in sorted(class_names.keys()):\n        count = split_counts.get(cls_id, 0)\n        print(f\"   - {class_names[cls_id]:<18}: {count} จุด/กรอบ\")\n    print(\"-\" * 35)\n\n# แสดงผลรวมทั้งหมด\nprint(\"\\n🏆 สรุปยอดรวมทั้งหมด (All Splits)\")\nprint(\"=\" * 35)\nfor cls_id in sorted(class_names.keys()):\n    count = total_counts.get(cls_id, 0)\n    print(f\"{class_names[cls_id]:<20}: {count} จุด/กรอบ\")\nprint(\"=\" * 35)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-05-24T13:31:21.218991Z","iopub.execute_input":"2026-05-24T13:31:21.219321Z","iopub.status.idle":"2026-05-24T13:31:21.307356Z","shell.execute_reply.started":"2026-05-24T13:31:21.219293Z","shell.execute_reply":"2026-05-24T13:31:21.306376Z"}},"outputs":[{"name":"stdout","text":"📊 กำลังตรวจสอบจำนวนข้อมูลแต่ละคลาส...\n\n📁 โฟลเดอร์: TRAIN\n   - Bacterial_Blight  : 136 จุด/กรอบ\n   - Brown_Spot        : 999 จุด/กรอบ\n   - Rice_Blast        : 172 จุด/กรอบ\n   - Narrow_Brown_Spot : 1172 จุด/กรอบ\n   - False_Smut        : 566 จุด/กรอบ\n   - Dirty_Seed        : 1234 จุด/กรอบ\n   - Sheath_Rot        : 382 จุด/กรอบ\n   - Stem_Rot          : 206 จุด/กรอบ\n   - Red_Stripe        : 179 จุด/กรอบ\n   - Bakanae           : 192 จุด/กรอบ\n-----------------------------------\n📁 โฟลเดอร์: VALID\n   - Bacterial_Blight  : 34 จุด/กรอบ\n   - Brown_Spot        : 285 จุด/กรอบ\n   - Rice_Blast        : 57 จุด/กรอบ\n   - Narrow_Brown_Spot : 77 จุด/กรอบ\n   - False_Smut        : 147 จุด/กรอบ\n   - Dirty_Seed        : 279 จุด/กรอบ\n   - Sheath_Rot        : 49 จุด/กรอบ\n   - Stem_Rot          : 51 จุด/กรอบ\n   - Red_Stripe        : 17 จุด/กรอบ\n   - Bakanae           : 70 จุด/กรอบ\n-----------------------------------\n📁 โฟลเดอร์: TEST\n   - Bacterial_Blight  : 25 จุด/กรอบ\n   - Brown_Spot        : 204 จุด/กรอบ\n   - Rice_Blast        : 44 จุด/กรอบ\n   - Narrow_Brown_Spot : 47 จุด/กรอบ\n   - False_Smut        : 69 จุด/กรอบ\n   - Dirty_Seed        : 114 จุด/กรอบ\n   - Sheath_Rot        : 24 จุด/กรอบ\n   - Stem_Rot          : 22 จุด/กรอบ\n   - Red_Stripe        : 16 จุด/กรอบ\n   - Bakanae           : 20 จุด/กรอบ\n-----------------------------------\n\n🏆 สรุปยอดรวมทั้งหมด (All Splits)\n===================================\nBacterial_Blight    : 195 จุด/กรอบ\nBrown_Spot          : 1488 จุด/กรอบ\nRice_Blast          : 273 จุด/กรอบ\nNarrow_Brown_Spot   : 1296 จุด/กรอบ\nFalse_Smut          : 782 จุด/กรอบ\nDirty_Seed          : 1627 จุด/กรอบ\nSheath_Rot          : 455 จุด/กรอบ\nStem_Rot            : 279 จุด/กรอบ\nRed_Stripe          : 212 จุด/กรอบ\nBakanae             : 282 จุด/กรอบ\n===================================\n","output_type":"stream"}],"execution_count":6},{"cell_type":"code","source":"import os\nimport glob\nfrom collections import Counter\n\n# 1. ระบุโฟลเดอร์ Dataset ปลายทางของคุณ\nDATASET_PATH = '/kaggle/input/datasets/switchonchannel/9dataset-v9-polygon-test'\n\n# 2. ระบุชื่อคลาสให้ตรงกับ data.yaml\nclass_names = {\n    0: 'Bacterial_Blight',\n    1: 'Brown_Spot',\n    2: 'Rice_Blast',\n    3: 'Narrow_Brown_Spot',\n    4: 'False_Smut',\n    5: 'Dirty_Seed',\n    6: 'Sheath_Rot',\n    7: 'Stem_Rot',\n    8: 'Red_Stripe',\n}\n\nsplits = ['train', 'valid', 'test']\ntotal_images_per_class = Counter()\ntotal_images = 0\n\nprint(\"📊 กำลังตรวจสอบจำนวน 'รูปภาพ' ในแต่ละคลาส...\\n\")\n\nfor split in splits:\n    label_dir = os.path.join(DATASET_PATH, split, 'labels')\n    split_images_per_class = Counter()\n    split_total_images = 0\n    \n    if os.path.exists(label_dir):\n        # ดึงไฟล์ .txt ทั้งหมด (ซึ่งมีจำนวนเท่ากับไฟล์รูปภาพ)\n        txt_files = glob.glob(os.path.join(label_dir, '*.txt'))\n        split_total_images = len(txt_files)\n        total_images += split_total_images\n        \n        for txt_file in txt_files:\n            classes_in_this_image = set() # ใช้ set เพื่อเก็บคลาสที่ไม่ซ้ำใน 1 รูป\n            with open(txt_file, 'r') as f:\n                lines = f.readlines()\n                for line in lines:\n                    parts = line.strip().split()\n                    if parts:\n                        try:\n                            class_id = int(parts[0])\n                            classes_in_this_image.add(class_id)\n                        except ValueError:\n                            pass\n            \n            # นำคลาสที่พบในรูปนี้ไปบวกเพิ่ม (บวกแค่คลาสละ 1 ครั้งต่อ 1 รูป)\n            for cls_id in classes_in_this_image:\n                split_images_per_class[cls_id] += 1\n                total_images_per_class[cls_id] += 1\n                            \n    print(f\"📁 โฟลเดอร์: {split.upper()} (มีรูปทั้งหมด {split_total_images} รูป)\")\n    for cls_id in sorted(class_names.keys()):\n        count = split_images_per_class.get(cls_id, 0)\n        print(f\"   - {class_names[cls_id]:<18}: {count} รูป\")\n    print(\"-\" * 45)\n\nprint(\"\\n🏆 สรุปยอดรวมทั้งหมด (All Splits)\")\nprint(\"=\" * 45)\nprint(f\"🖼️ รวมรูปภาพทั้งหมดใน Dataset: {total_images} รูป\")\nprint(\"-\" * 45)\nfor cls_id in sorted(class_names.keys()):\n    count = total_images_per_class.get(cls_id, 0)\n    print(f\"{class_names[cls_id]:<20}: {count} รูป\")\nprint(\"=\" * 45)\nprint(\"*หมายเหตุ: 1 รูปภาพอาจมีหลายโรครวมกันได้ ตัวเลขรวมของแต่ละโรคจึงอาจมากกว่าจำนวนรูปภาพทั้งหมด\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-09-17T07:34:14.550077Z","iopub.execute_input":"2026-09-17T07:34:14.55034Z","iopub.status.idle":"2026-09-17T07:34:23.357035Z","shell.execute_reply.started":"2026-09-17T07:34:14.550313Z","shell.execute_reply":"2026-09-17T07:34:23.356109Z"}},"outputs":[{"name":"stdout","text":"📊 กำลังตรวจสอบจำนวน 'รูปภาพ' ในแต่ละคลาส...\n\n📁 โฟลเดอร์: TRAIN (มีรูปทั้งหมด 1662 รูป)\n   - Bacterial_Blight  : 242 รูป\n   - Brown_Spot        : 160 รูป\n   - Rice_Blast        : 156 รูป\n   - Narrow_Brown_Spot : 303 รูป\n   - False_Smut        : 237 รูป\n   - Dirty_Seed        : 220 รูป\n   - Sheath_Rot        : 138 รูป\n   - Stem_Rot          : 57 รูป\n   - Red_Stripe        : 55 รูป\n---------------------------------------------\n📁 โฟลเดอร์: VALID (มีรูปทั้งหมด 300 รูป)\n   - Bacterial_Blight  : 35 รูป\n   - Brown_Spot        : 36 รูป\n   - Rice_Blast        : 27 รูป\n   - Narrow_Brown_Spot : 19 รูป\n   - False_Smut        : 59 รูป\n   - Dirty_Seed        : 30 รูป\n   - Sheath_Rot        : 19 รูป\n   - Stem_Rot          : 16 รูป\n   - Red_Stripe        : 11 รูป\n---------------------------------------------\n📁 โฟลเดอร์: TEST (มีรูปทั้งหมด 183 รูป)\n   - Bacterial_Blight  : 27 รูป\n   - Brown_Spot        : 25 รูป\n   - Rice_Blast        : 18 รูป\n   - Narrow_Brown_Spot : 14 รูป\n   - False_Smut        : 30 รูป\n   - Dirty_Seed        : 16 รูป\n   - Sheath_Rot        : 10 รูป\n   - Stem_Rot          : 8 รูป\n   - Red_Stripe        : 5 รูป\n---------------------------------------------\n\n🏆 สรุปยอดรวมทั้งหมด (All Splits)\n=============================================\n🖼️ รวมรูปภาพทั้งหมดใน Dataset: 2145 รูป\n---------------------------------------------\nBacterial_Blight    : 304 รูป\nBrown_Spot          : 221 รูป\nRice_Blast          : 201 รูป\nNarrow_Brown_Spot   : 336 รูป\nFalse_Smut          : 326 รูป\nDirty_Seed          : 266 รูป\nSheath_Rot          : 167 รูป\nStem_Rot            : 81 รูป\nRed_Stripe          : 71 รูป\n=============================================\n*หมายเหตุ: 1 รูปภาพอาจมีหลายโรครวมกันได้ ตัวเลขรวมของแต่ละโรคจึงอาจมากกว่าจำนวนรูปภาพทั้งหมด\n","output_type":"stream"}],"execution_count":1},{"cell_type":"code","source":"import os\nimport glob\nimport shutil\nimport re\n\n# 1. กำหนดโฟลเดอร์ต้นทาง (Read-only) และปลายทาง (ที่จะสร้างใหม่)\nSRC_DIR = '/kaggle/input/datasets/switchonchannel/7datav21'\nDST_DIR = '/kaggle/working/7dataset_cleaned' # ข้อมูลชุดใหม่จะอยู่ที่นี่\n\nTARGET_CLASS = 5  # Dirty_Seed\nREMOVE_COUNT = 15\n\nsplits = ['train', 'valid', 'test']\n\nprint(\"🚀 เริ่มต้นสร้าง Dataset ใหม่แบบหัก Dirty_Seed ใน Valid ออก 15 รูป...\\n\")\n\nfor split in splits:\n    src_img_dir = os.path.join(SRC_DIR, split, 'images')\n    src_lbl_dir = os.path.join(SRC_DIR, split, 'labels')\n    \n    dst_img_dir = os.path.join(DST_DIR, split, 'images')\n    dst_lbl_dir = os.path.join(DST_DIR, split, 'labels')\n    \n    os.makedirs(dst_img_dir, exist_ok=True)\n    os.makedirs(dst_lbl_dir, exist_ok=True)\n    \n    txt_files = glob.glob(os.path.join(src_lbl_dir, '*.txt'))\n    skipped_count = 0\n    copied_count = 0\n    \n    print(f\"📁 กำลังจัดการโฟลเดอร์: {split.upper()}\")\n    \n    for txt_path in txt_files:\n        filename = os.path.basename(txt_path)\n        base_name = os.path.splitext(filename)[0]\n        \n        # ตรวจสอบเงื่อนไข: ถ้าเป็นโฟลเดอร์ valid และยังข้ามไม่ครบ 15 รูป\n        skip_this_file = False\n        if split == 'valid' and skipped_count < REMOVE_COUNT:\n            with open(txt_path, 'r') as f:\n                lines = f.readlines()\n                \n            for line in lines:\n                parts = line.strip().split()\n                if parts and int(parts[0]) == TARGET_CLASS:\n                    skip_this_file = True\n                    break\n        \n        # ค้นหาไฟล์รูปภาพที่นามสกุลตรงกัน\n        img_path = None\n        for ext in ['.jpg', '.png', '.jpeg', '.JPG', '.PNG']:\n            temp_img = os.path.join(src_img_dir, base_name + ext)\n            if os.path.exists(temp_img):\n                img_path = temp_img\n                break\n                \n        if skip_this_file and img_path:\n            skipped_count += 1\n            print(f\"  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): {base_name} [{skipped_count}/{REMOVE_COUNT}]\")\n        else:\n            # ก๊อปปี้ไฟล์ปกติ\n            if img_path:\n                shutil.copy(img_path, os.path.join(dst_img_dir, os.path.basename(img_path)))\n                shutil.copy(txt_path, os.path.join(dst_lbl_dir, filename))\n                copied_count += 1\n                \n    print(f\"  ✅ ก๊อปปี้ไปทั้งหมด {copied_count} รูป\\n\")\n\n# 2. ก๊อปปี้และอัปเดตไฟล์ data.yaml\nyaml_src = os.path.join(SRC_DIR, 'data.yaml')\nif os.path.exists(yaml_src):\n    with open(yaml_src, 'r') as f:\n        yaml_content = f.read()\n    # แก้ไข path ใน data.yaml ให้ชี้ไปที่โฟลเดอร์ใหม่\n    yaml_content = re.sub(r'path:.*', f'path: {DST_DIR}', yaml_content)\n    with open(os.path.join(DST_DIR, 'data.yaml'), 'w') as f:\n        f.write(yaml_content)\n\nprint(\"-\" * 50)\nprint(f\"🎯 เสร็จสมบูรณ์! Dataset ใหม่ของคุณพร้อมใช้งานอยู่ที่: {DST_DIR}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-05-26T01:12:46.479438Z","iopub.execute_input":"2026-05-26T01:12:46.480238Z","iopub.status.idle":"2026-05-26T01:13:01.916574Z","shell.execute_reply.started":"2026-05-26T01:12:46.4802Z","shell.execute_reply":"2026-05-26T01:13:01.915505Z"}},"outputs":[{"name":"stdout","text":"🚀 เริ่มต้นสร้าง Dataset ใหม่แบบหัก Dirty_Seed ใน Valid ออก 15 รูป...\n\n📁 กำลังจัดการโฟลเดอร์: TRAIN\n  ✅ ก๊อปปี้ไปทั้งหมด 1199 รูป\n\n📁 กำลังจัดการโฟลเดอร์: VALID\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_-5-1024x768_jpg.rf.aafe6384e0a98fec12f781236ebd8ab1 [1/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_Screenshot-2026-04-02-003544_png_jpg.rf.b0dc38fd89926c5c49ff54488d5635da [2/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_1774602824967_jpg.rf.3895d71819e62a305ea05f86e914dcc8 [3/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_Screenshot-2026-04-02-003812_png_jpg.rf.0af68a786c99c0725c0d97f6295fd5d8 [4/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-151642_png_jpg.rf.cbc01d05193184038de00de7d2e42769 [5/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-152921_png_jpg.rf.20ed999ced4e3a0f2be2d15b37b84e68 [6/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_Screenshot-2026-04-02-003725_png_jpg.rf.47025b4fc0488d5e0569976de29fe263 [7/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_1774602818838_jpg.rf.5c23c8c070c4047724cc465091715ae2 [8/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-151941_png_jpg.rf.42623fdf843f717091551c5a432e0a30 [9/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_f681ff7c44632177fb26544048130164_jpg.rf.45eeb80c2e18da0cba989637940f1633 [10/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-151953_png_jpg.rf.8aeb3d698725281a4d166c1ee06e0e68 [11/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_Screenshot-2026-04-02-003749_png_jpg.rf.5fdd9d52c3f5cf653b8be3667038ef21 [12/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-153157-Copy_png_jpg.rf.73c6295069adac6faa02147e5d6f7079 [13/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d3_1774602818838_jpg.rf.a91d8536047cfea170d74f0f0e0e9b78 [14/15]\n  ❌ ข้ามไฟล์ (ไม่ก๊อปปี้): d2_d2_Screenshot-2026-04-01-152807_png_jpg.rf.6ed2d948956da66a0d452bcedf278508 [15/15]\n  ✅ ก๊อปปี้ไปทั้งหมด 199 รูป\n\n📁 กำลังจัดการโฟลเดอร์: TEST\n  ✅ ก๊อปปี้ไปทั้งหมด 130 รูป\n\n--------------------------------------------------\n🎯 เสร็จสมบูรณ์! Dataset ใหม่ของคุณพร้อมใช้งานอยู่ที่: /kaggle/working/7dataset_cleaned\n","output_type":"stream"}],"execution_count":2},{"cell_type":"code","source":"delete bakanae","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import os\nimport glob\nimport shutil\n\n# 1. กำหนดพาธต้นทาง (Read-only) และพาธปลายทางสำหรับทำงาน (Read-Write)\nsource_path = '/kaggle/input/datasets/switchonchannel/10-data-v2delete'\nworking_path = '/kaggle/working/10dataset_v2_clean'\n\n# 2. คัดลอกข้อมูลทั้งหมดมาไว้ที่ /kaggle/working/\nprint(\"⏳ กำลัง Copy ชุดข้อมูลไปยังพื้นที่ทำงาน (/kaggle/working) ...\")\nif os.path.exists(working_path):\n    shutil.rmtree(working_path) # เคลียร์โฟลเดอร์เก่าทิ้งถ้ามี\nshutil.copytree(source_path, working_path)\nprint(\"✅ Copy เสร็จเรียบร้อย! เริ่มกระบวนการเคลียร์ Class 9...\")\n\n# 3. เริ่มกระบวนการลบคลาส 9 บน working_path ที่เราเพิ่งก๊อปปี้มา\nsplits = ['train', 'valid', 'test']\nclass_to_remove = '9'\n\nremoved_bboxes = 0\nremoved_images = 0\n\nfor split in splits:\n    label_dir = os.path.join(working_path, split, 'labels')\n    image_dir = os.path.join(working_path, split, 'images')\n\n    if not os.path.exists(label_dir):\n        continue\n\n    label_files = glob.glob(os.path.join(label_dir, '*.txt'))\n\n    for txt_file in label_files:\n        with open(txt_file, 'r') as f:\n            lines = f.readlines()\n\n        new_lines = [line for line in lines if not line.startswith(f'{class_to_remove} ')]\n        removed_bboxes += (len(lines) - len(new_lines))\n\n        # ถ้ารูปนี้มีแค่คลาส 9 อย่างเดียว\n        if len(lines) > 0 and len(new_lines) == 0:\n            base_name = os.path.splitext(os.path.basename(txt_file))[0]\n            \n            for ext in ['.jpg', '.jpeg', '.png', '.JPG', '.JPEG', '.PNG']:\n                img_path = os.path.join(image_dir, base_name + ext)\n                if os.path.exists(img_path):\n                    os.remove(img_path)\n                    removed_images += 1\n                    break\n            \n            os.remove(txt_file)\n        else:\n            # ถ้ายังมีโรคคลาสอื่นปนอยู่ ให้เขียนทับ (ตอนนี้เขียนได้แล้วเพราะอยู่ใน /working)\n            with open(txt_file, 'w') as f:\n                f.writelines(new_lines)\n\nprint(\"-\" * 50)\nprint(f\"✅ ลบ Bounding Box ของคลาส 9 ออกทั้งหมด: {removed_bboxes} กล่อง\")\nprint(f\"✅ ลบรูปภาพที่มีเฉพาะคลาส 9 ทิ้งไป: {removed_images} รูป\")\nprint(f\"🎉 Dataset ใหม่ที่พร้อมใช้งานอยู่ที่: {working_path}\")\nprint(\"-\" * 50)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-06-23T07:27:39.902857Z","iopub.execute_input":"2026-06-23T07:27:39.903212Z","iopub.status.idle":"2026-06-23T07:27:59.106618Z","shell.execute_reply.started":"2026-06-23T07:27:39.903186Z","shell.execute_reply":"2026-06-23T07:27:59.105677Z"}},"outputs":[{"name":"stdout","text":"⏳ กำลัง Copy ชุดข้อมูลไปยังพื้นที่ทำงาน (/kaggle/working) ...\n✅ Copy เสร็จเรียบร้อย! เริ่มกระบวนการเคลียร์ Class 9...\n--------------------------------------------------\n✅ ลบ Bounding Box ของคลาส 9 ออกทั้งหมด: 282 กล่อง\n✅ ลบรูปภาพที่มีเฉพาะคลาส 9 ทิ้งไป: 73 รูป\n🎉 Dataset ใหม่ที่พร้อมใช้งานอยู่ที่: /kaggle/working/10dataset_v2_clean\n--------------------------------------------------\n","output_type":"stream"}],"execution_count":2},{"cell_type":"code","source":"%%writefile /kaggle/working/10dataset_v2_clean/data.yaml\npath: /kaggle/working/10dataset_v2_clean\ntrain: train/images\nval: valid/images\ntest: test/images\n\nnc: 9\nnames:\n  0: Bacterial_Blight\n  1: Brown_Spot\n  2: Rice_Blast\n  3: Narrow_Brown_Spot\n  4: False_Smut\n  5: Dirty_Seed\n  6: Sheath_Rot\n  7: Stem_Rot\n  8: Red_Stripe","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-06-23T07:29:09.381622Z","iopub.execute_input":"2026-06-23T07:29:09.382191Z","iopub.status.idle":"2026-06-23T07:29:09.388153Z","shell.execute_reply.started":"2026-06-23T07:29:09.382159Z","shell.execute_reply":"2026-06-23T07:29:09.387226Z"}},"outputs":[{"name":"stdout","text":"Overwriting /kaggle/working/10dataset_v2_clean/data.yaml\n","output_type":"stream"}],"execution_count":3},{"cell_type":"code","source":"zip file","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import shutil\nimport os\n\n# กำหนดพาธโฟลเดอร์ต้นทาง และชื่อไฟล์ ZIP ปลายทาง\nfolder_to_zip = '/kaggle/working/9v4_clean_with_bg'\noutput_zip_name = '/kaggle/working/9dataset_v9_Polygon'\n\nprint(\"⏳ กำลังเริ่มบีบอัดโฟลเดอร์เป็นไฟล์ ZIP... (อาจใช้เวลาสักครู่ขึ้นอยู่กับขนาดของรูปภาพ)\")\n\n# สั่งบีบอัดโฟลเดอร์ (ระบบจะเติมนามสกุล .zip ให้โดยอัตโนมัติ)\nshutil.make_archive(output_zip_name, 'zip', folder_to_zip)\n\n# ตรวจสอบขนาดไฟล์ที่ได้\nzip_file_path = f\"{output_zip_name}.zip\"\nfile_size_gb = os.path.getsize(zip_file_path) / (1024 * 1024 * 1024)\n\nprint(\"-\" * 50)\nprint(f\"✅ บีบอัดไฟล์เสร็จสมบูรณ์!\")\nprint(f\"📦 ไฟล์ ZIP ของคุณอยู่ที่: {zip_file_path}\")\nprint(f\"📂 ขนาดไฟล์: {file_size_gb:.2f} GB\")\nprint(\"-\" * 50)\nprint(\"💡 ทริค: คุณสามารถดาวน์โหลดไฟล์นี้ได้จากแถบด้านขวา (Output) ของ Kaggle ได้เลยครับ\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-09-03T08:59:28.572352Z","iopub.execute_input":"2026-09-03T08:59:28.572704Z","iopub.status.idle":"2026-09-03T08:59:42.508647Z","shell.execute_reply.started":"2026-09-03T08:59:28.572674Z","shell.execute_reply":"2026-09-03T08:59:42.507077Z"}},"outputs":[{"name":"stdout","text":"⏳ กำลังเริ่มบีบอัดโฟลเดอร์เป็นไฟล์ ZIP... (อาจใช้เวลาสักครู่ขึ้นอยู่กับขนาดของรูปภาพ)\n--------------------------------------------------\n✅ บีบอัดไฟล์เสร็จสมบูรณ์!\n📦 ไฟล์ ZIP ของคุณอยู่ที่: /kaggle/working/9dataset_v9_Polygon.zip\n📂 ขนาดไฟล์: 0.23 GB\n--------------------------------------------------\n💡 ทริค: คุณสามารถดาวน์โหลดไฟล์นี้ได้จากแถบด้านขวา (Output) ของ Kaggle ได้เลยครับ\n","output_type":"stream"}],"execution_count":2},{"cell_type":"code","source":"เพิ่ม dataset picture ","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import os\nimport glob\nimport shutil\n\n# 1. กำหนดพาธทั้งหมด\n# พาธ Dataset หลัก (9 คลาส) ที่เป็น Read-only\nmain_input_path = '/kaggle/input/datasets/switchonchannel/9v3-clean' \n\n# ⚠️ อย่าลืมเปลี่ยนตรงนี้เป็นพาธของ Dataset ใหม่ที่คุณเพิ่งโหลดมา (มี 3 คลาส)\nnew_dataset_path = '/kaggle/input/datasets/switchonchannel/44batirea/battereai44.v1i.yolo26' \n\n# พาธพื้นที่ทำงานที่เราจะเอาทั้งสอง Dataset มารวมร่างกัน (Read/Write)\nworking_merged_path = '/kaggle/working/merged_dataset_final'\n\n\n# 2. คัดลอก Dataset หลักมาไว้ที่พื้นที่ทำงานก่อน (เพื่อปลดล็อก Read-only)\nprint(\"⏳ [1/2] กำลัง Copy Dataset หลักมายังพื้นที่ทำงาน (/kaggle/working)...\")\nif os.path.exists(working_merged_path):\n    shutil.rmtree(working_merged_path) # เคลียร์โฟลเดอร์เก่าถ้ามี\nshutil.copytree(main_input_path, working_merged_path)\nprint(\"✅ Copy Dataset หลักสำเร็จ! พร้อมสำหรับการผสมข้อมูลแล้ว\")\n\n\n# 3. แปลงคลาสและนำ Dataset ใหม่มาผสม\nclass_map = {\n    '0': '0'  # ชุดใหม่ 0 (brown_spot) -> ชุดหลัก 1\n}\n\nsplits = ['train', 'valid', 'test']\ncopied_images = 0\nremapped_labels = 0\n\nprint(\"⏳ [2/2] กำลังแปลง Class ID และนำภาพใหม่มาผสม...\")\n\nfor split in splits:\n    # พาธโฟลเดอร์ของชุดข้อมูลใหม่\n    new_label_dir = os.path.join(new_dataset_path, split, 'labels')\n    new_image_dir = os.path.join(new_dataset_path, split, 'images')\n\n    # พาธโฟลเดอร์ของชุดข้อมูลหลักที่ก๊อปปี้มาแล้ว (ตอนนี้เขียนทับได้แล้ว)\n    main_label_dir = os.path.join(working_merged_path, split, 'labels')\n    main_image_dir = os.path.join(working_merged_path, split, 'images')\n\n    if not os.path.exists(new_label_dir):\n        continue\n\n    txt_files = glob.glob(os.path.join(new_label_dir, '*.txt'))\n\n    for txt_file in txt_files:\n        with open(txt_file, 'r') as f:\n            lines = f.readlines()\n\n        new_lines = []\n        for line in lines:\n            parts = line.strip().split()\n            if len(parts) >= 5:\n                old_class_id = parts[0]\n                if old_class_id in class_map:\n                    new_class_id = class_map[old_class_id]\n                    new_line = f\"{new_class_id} {' '.join(parts[1:])}\\n\"\n                    new_lines.append(new_line)\n\n        # เขียนไฟล์ label ลงไปในโฟลเดอร์ทำงานใหม่\n        if new_lines:\n            base_name = os.path.basename(txt_file)\n            dest_label_path = os.path.join(main_label_dir, base_name)\n            \n            with open(dest_label_path, 'w') as f:\n                f.writelines(new_lines)\n            remapped_labels += 1\n\n            # ก๊อปปี้รูปตามมา\n            img_name_base = os.path.splitext(base_name)[0]\n            for ext in ['.jpg', '.jpeg', '.png', '.JPG', '.JPEG', '.PNG']:\n                src_img_path = os.path.join(new_image_dir, img_name_base + ext)\n                if os.path.exists(src_img_path):\n                    dest_img_path = os.path.join(main_image_dir, img_name_base + ext)\n                    shutil.copy(src_img_path, dest_img_path)\n                    copied_images += 1\n                    break\n\nprint(\"-\" * 50)\nprint(f\"✅ แปลง Label สำเร็จ: {remapped_labels} ไฟล์\")\nprint(f\"✅ คัดลอกรูปภาพใหม่สำเร็จ: {copied_images} รูป\")\nprint(f\"🎉 Dataset ที่รวมกันเสร็จสมบูรณ์อยู่ที่: {working_merged_path}\")\nprint(\"-\" * 50)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-06-23T12:12:21.742041Z","iopub.execute_input":"2026-06-23T12:12:21.743096Z","iopub.status.idle":"2026-06-23T12:12:37.300108Z","shell.execute_reply.started":"2026-06-23T12:12:21.743044Z","shell.execute_reply":"2026-06-23T12:12:37.299186Z"}},"outputs":[{"name":"stdout","text":"⏳ [1/2] กำลัง Copy Dataset หลักมายังพื้นที่ทำงาน (/kaggle/working)...\n✅ Copy Dataset หลักสำเร็จ! พร้อมสำหรับการผสมข้อมูลแล้ว\n⏳ [2/2] กำลังแปลง Class ID และนำภาพใหม่มาผสม...\n--------------------------------------------------\n✅ แปลง Label สำเร็จ: 93 ไฟล์\n✅ คัดลอกรูปภาพใหม่สำเร็จ: 93 รูป\n🎉 Dataset ที่รวมกันเสร็จสมบูรณ์อยู่ที่: /kaggle/working/merged_dataset_final\n--------------------------------------------------\n","output_type":"stream"}],"execution_count":1},{"cell_type":"code","source":"import os\nimport shutil\n\n# ==========================================\n# 1. กำหนด Path ต้นทางและปลายทาง\n# ==========================================\nsource_dataset_dir = '/kaggle/input/datasets/switchonchannel/9dataset-v8-poly' \nhealthy_source_dir = '/kaggle/input/datasets/switchonchannel/bg-bb2/BG_2'\n\nworking_dataset_dir = '/kaggle/working/9v4_clean_with_bg'\n\n# ==========================================\n# 2. คัดลอก Dataset เดิมมาไว้ที่ Working Directory\n# ==========================================\nprint(\"กำลังคัดลอก Dataset เดิมไปยังพื้นที่ทำงาน...\")\nif not os.path.exists(working_dataset_dir):\n    shutil.copytree(source_dataset_dir, working_dataset_dir)\nelse:\n    print(\"โฟลเดอร์ปลายทางมีอยู่แล้ว จะดำเนินการเพิ่มไฟล์ต่อเลย\")\n\n# ==========================================\n# 3. ดึงรายชื่อภาพ Healthy Rice Leaf ทั้งหมด\n# ==========================================\nall_healthy_images = [f for f in os.listdir(healthy_source_dir) if f.lower().endswith(('.png', '.jpg', '.jpeg'))]\n\n# ==========================================\n# 4. ย้ายภาพทั้งหมดเข้าโฟลเดอร์ train และสร้างไฟล์ Label เปล่า\n# ==========================================\nprint(f\"กำลังย้ายภาพ Background ทั้งหมดจำนวน {len(all_healthy_images)} รูปไปยังโฟลเดอร์ train (พร้อมเติม prefix 'bg_')...\")\n\ntarget_images_dir = os.path.join(working_dataset_dir, 'train', 'images')\ntarget_labels_dir = os.path.join(working_dataset_dir, 'train', 'labels')\n\nos.makedirs(target_images_dir, exist_ok=True)\nos.makedirs(target_labels_dir, exist_ok=True)\n\nfor img_name in all_healthy_images:\n    # เติม prefix 'bg_' เพื่อป้องกันชื่อไฟล์ซ้ำกับข้อมูลเดิม\n    new_img_name = 'bg_2' + img_name\n    \n    # 4.1 คัดลอกรูปภาพ\n    src_img_path = os.path.join(healthy_source_dir, img_name)\n    dst_img_path = os.path.join(target_images_dir, new_img_name)\n    shutil.copy(src_img_path, dst_img_path)\n    \n    # 4.2 สร้างไฟล์ Label เปล่า (.txt) โดยอิงจากชื่อใหม่\n    base_name = os.path.splitext(new_img_name)[0]\n    label_name = base_name + '.txt'\n    dst_label_path = os.path.join(target_labels_dir, label_name)\n    \n    # สร้างไฟล์ 0 byte\n    with open(dst_label_path, 'w') as f:\n        pass\n\nprint(f\"เสร็จสิ้น! ตอนนี้ Dataset ใหม่พร้อมใช้งานอยู่ที่: {working_dataset_dir}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-09-03T08:55:47.62106Z","iopub.execute_input":"2026-09-03T08:55:47.621372Z","iopub.status.idle":"2026-09-03T08:56:15.274423Z","shell.execute_reply.started":"2026-09-03T08:55:47.621333Z","shell.execute_reply":"2026-09-03T08:56:15.273367Z"}},"outputs":[{"name":"stdout","text":"กำลังคัดลอก Dataset เดิมไปยังพื้นที่ทำงาน...\nกำลังย้ายภาพ Background ทั้งหมดจำนวน 40 รูปไปยังโฟลเดอร์ train (พร้อมเติม prefix 'bg_')...\nเสร็จสิ้น! ตอนนี้ Dataset ใหม่พร้อมใช้งานอยู่ที่: /kaggle/working/9v4_clean_with_bg\n","output_type":"stream"}],"execution_count":1},{"cell_type":"code","source":"รวม data ","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import os\nimport shutil\nimport textwrap\n\n# ==========================================\n# 1. กำหนด Path ของ Dataset\n# ==========================================\nds_main_path = '/kaggle/input/datasets/switchonchannel/9dataset-v5-cleanbg' \nds_sec_path = '/kaggle/input/datasets/switchonchannel/premj4-v1-mai'   \n# เปลี่ยนชื่อโฟลเดอร์ให้ตรงกับรูป (combi_dataset1)\noutput_path = '/kaggle/working/combi_dataset2'\n\n# ==========================================\n# 2. กำหนด Dictionary สำหรับแปลง Class ID (nc:4 -> nc:9)\n# ==========================================\nclass_mapping = {\n    0: 0, # Bacterial_Blight\n    1: 1, # Brown_Spot\n    2: 5, # Dirty_Seed\n    3: 2  # Rice_Blast\n}\n\n# กำหนด Splits แยกกันสำหรับ Dataset หลักและรอง\nsplits_main = ['train', 'valid', 'test'] # ดึงครบทุกส่วนจาก Dataset หลัก\nsplits_sec = ['train']                   # ดึงแค่ train จาก Dataset รอง\n\ndef create_dirs(base_path):\n    for split in splits_main: # สร้างโฟลเดอร์เตรียมไว้ให้ครบทั้ง 3\n        os.makedirs(os.path.join(base_path, split, 'images'), exist_ok=True)\n        os.makedirs(os.path.join(base_path, split, 'labels'), exist_ok=True)\n\nprint(f\"กำลังสร้างโครงสร้างโฟลเดอร์ใหม่ที่: {output_path} ...\")\ncreate_dirs(output_path)\n\n# ==========================================\n# 3. ฟังก์ชันสำหรับคัดลอกและแปลงข้อมูล\n# ==========================================\ndef process_dataset(source_path, split, is_main_dataset=True):\n    # รองรับกรณีที่ Dataset ต้นทางใช้ชื่อโฟลเดอร์ว่า 'val' แทน 'valid'\n    split_name = 'val' if (split == 'valid' and not os.path.exists(os.path.join(source_path, 'valid'))) else split\n    \n    img_dir = os.path.join(source_path, split_name, 'images')\n    lbl_dir = os.path.join(source_path, split_name, 'labels')\n    \n    if not os.path.exists(img_dir):\n        print(f\"ข้าม: ไม่พบโฟลเดอร์ {img_dir} (อาจไม่มีอยู่จริง)\")\n        return\n\n    out_img_dir = os.path.join(output_path, split, 'images')\n    out_lbl_dir = os.path.join(output_path, split, 'labels')\n\n    images = [f for f in os.listdir(img_dir) if f.endswith(('.jpg', '.png', '.jpeg', '.JPG', '.JPEG', '.PNG'))]\n    \n    for img_name in images:\n        base_name = os.path.splitext(img_name)[0]\n        lbl_name = base_name + '.txt'\n        \n        prefix = \"\" if is_main_dataset else \"ds2_\"\n        new_img_name = prefix + img_name\n        new_lbl_name = prefix + lbl_name\n        \n        img_src = os.path.join(img_dir, img_name)\n        img_dst = os.path.join(out_img_dir, new_img_name)\n        shutil.copy(img_src, img_dst)\n        \n        src_lbl_path = os.path.join(lbl_dir, lbl_name)\n        dst_lbl_path = os.path.join(out_lbl_dir, new_lbl_name)\n        \n        if os.path.exists(src_lbl_path):\n            if is_main_dataset:\n                shutil.copy(src_lbl_path, dst_lbl_path)\n            else:\n                valid_boxes = 0 \n                with open(src_lbl_path, 'r') as f_in, open(dst_lbl_path, 'w') as f_out:\n                    for line in f_in:\n                        parts = line.strip().split()\n                        if len(parts) >= 5:\n                            old_class = int(float(parts[0])) \n                            if old_class in class_mapping:\n                                new_class = class_mapping[old_class]\n                                parts[0] = str(new_class)\n                                f_out.write(\" \".join(parts) + \"\\n\")\n                                valid_boxes += 1\n                \n                if valid_boxes == 0:\n                    os.remove(dst_lbl_path) \n                    os.remove(img_dst)      \n        else:\n            os.remove(img_dst)\n\n# เริ่มการทำงานทีละชุด\nprint(\"\\n--- 1. ดึงข้อมูลจาก Dataset หลัก (I'NINE) ---\")\nfor split in splits_main:\n    print(f\"-> กำลังคัดลอก: {split}\")\n    process_dataset(ds_main_path, split, is_main_dataset=True)\n\nprint(\"\\n--- 2. ดึงข้อมูลจาก Dataset รอง ---\")\nfor split in splits_sec:\n    print(f\"-> กำลังคัดลอกและแปลงคลาส: {split}\")\n    process_dataset(ds_sec_path, split, is_main_dataset=False)\n\n# ==========================================\n# 4. สร้างไฟล์ data.yaml ใหม่\n# ==========================================\n# เนื่องจากเราก็อปปี้ valid และ test มาไว้ในเครื่องแล้ว จึงเปลี่ยน Path ให้เป็นโฟลเดอร์ภายใน\nyaml_content = f\"\"\"\npath: {output_path}\ntrain: train/images\nval: valid/images\ntest: test/images\n\nnc: 9\nnames:\n  0: Bacterial_Blight\n  1: Brown_Spot\n  2: Rice_Blast\n  3: Narrow_Brown_Spot\n  4: False_Smut\n  5: Dirty_Seed\n  6: Sheath_Rot\n  7: Stem_Rot\n  8: Red_Stripe\n\"\"\"\n\nwith open(os.path.join(output_path, 'data.yaml'), 'w') as f:\n    f.write(textwrap.dedent(yaml_content).strip())\n\nprint(f\"\\n✅ รวม Dataset เสร็จสมบูรณ์! โฟลเดอร์และไฟล์ถูกจัดเก็บไว้ที่: {output_path}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-12T01:11:53.065779Z","iopub.execute_input":"2026-08-12T01:11:53.066723Z","iopub.status.idle":"2026-08-12T01:12:15.197339Z","shell.execute_reply.started":"2026-08-12T01:11:53.066688Z","shell.execute_reply":"2026-08-12T01:12:15.196375Z"}},"outputs":[{"name":"stdout","text":"กำลังสร้างโครงสร้างโฟลเดอร์ใหม่ที่: /kaggle/working/combi_dataset2 ...\n\n--- 1. ดึงข้อมูลจาก Dataset หลัก (I'NINE) ---\n-> กำลังคัดลอก: train\n-> กำลังคัดลอก: valid\n-> กำลังคัดลอก: test\n\n--- 2. ดึงข้อมูลจาก Dataset รอง ---\n-> กำลังคัดลอกและแปลงคลาส: train\n\n✅ รวม Dataset เสร็จสมบูรณ์! โฟลเดอร์และไฟล์ถูกจัดเก็บไว้ที่: /kaggle/working/combi_dataset2\n","output_type":"stream"}],"execution_count":3},{"cell_type":"code","source":"from collections import Counter\nimport os\n\n# รันเช็คเฉพาะ train ตามที่มีใน splits \noutput_path = '/kaggle/input/datasets/switchonchannel/9dataset-v8-poly'\nc = Counter()\nlbl_dir = f'{output_path}/train/labels'\nimg_dir = f'{output_path}/train/images'\n\n# นับคลาสในทุกไฟล์\nfor fn in os.listdir(lbl_dir):\n    with open(os.path.join(lbl_dir, fn), 'r') as f:\n        for line in f:\n            if line.strip(): \n                c[int(float(line.split()[0]))] += 1\n\nprint(f\"📊 สรุปผลการรวมข้อมูล (Train):\")\nprint(f\"Images: {len(os.listdir(img_dir))} ใบ\")\nprint(f\"Labels: {len(os.listdir(lbl_dir))} ไฟล์\")\nprint(\"-\" * 20)\n\nfor i in range(9): \n    print(f\"Class {i}: {c[i]}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-23T09:28:01.051462Z","iopub.execute_input":"2026-08-23T09:28:01.051797Z","iopub.status.idle":"2026-08-23T09:28:03.28479Z","shell.execute_reply.started":"2026-08-23T09:28:01.05177Z","shell.execute_reply":"2026-08-23T09:28:03.283898Z"}},"outputs":[{"name":"stdout","text":"📊 สรุปผลการรวมข้อมูล (Train):\nImages: 1622 ใบ\nLabels: 1622 ไฟล์\n--------------------\nClass 0: 658\nClass 1: 1256\nClass 2: 606\nClass 3: 1188\nClass 4: 566\nClass 5: 1340\nClass 6: 386\nClass 7: 206\nClass 8: 179\n","output_type":"stream"}],"execution_count":10},{"cell_type":"code","source":"import os\nfrom pathlib import Path\n\ndef count_background_images(dataset_path):\n    # ชื่อโฟลเดอร์ย่อยที่ต้องการเช็ค\n    splits = ['train', 'valid', 'test']\n    \n    # นามสกุลไฟล์ภาพที่รองรับ (เพิ่มลดได้ตามต้องการ)\n    valid_extensions = {'.jpg', '.jpeg', '.png', '.bmp', '.webp'}\n    \n    total_unlabeled_all = 0\n    total_images_all = 0\n\n    print(f\"--- เริ่มตรวจสอบ Dataset ที่: {dataset_path} ---\\n\")\n\n    for split in splits:\n        img_dir = Path(dataset_path) / split / 'images'\n        lbl_dir = Path(dataset_path) / split / 'labels'\n        \n        if not img_dir.exists():\n            print(f\"⚠️ ไม่พบโฟลเดอร์: {img_dir}\")\n            continue\n            \n        # สร้างโฟลเดอร์ labels เผื่อไว้ในกรณีที่ยังไม่มีเลย\n        if not lbl_dir.exists():\n            print(f\"⚠️ ไม่พบโฟลเดอร์ labels ใน {split} (ถือว่าไม่มี label ทั้งหมด)\")\n            lbl_dir.mkdir(parents=True, exist_ok=True)\n\n        unlabeled_count = 0\n        image_count = 0\n\n        # วนลูปอ่านไฟล์ภาพทั้งหมดในโฟลเดอร์ images\n        for img_path in img_dir.iterdir():\n            if img_path.suffix.lower() in valid_extensions:\n                image_count += 1\n                \n                # สร้าง path ของไฟล์ label ที่ควรจะมีชื่อเหมือนกันแต่เป็น .txt\n                lbl_path = lbl_dir / f\"{img_path.stem}.txt\"\n                \n                # เช็คว่า 1. ไม่มีไฟล์ label หรือ 2. มีไฟล์แต่ขนาด 0 bytes (ว่างเปล่า)\n                if not lbl_path.exists() or lbl_path.stat().st_size == 0:\n                    unlabeled_count += 1\n        \n        print(f\"📁 โฟลเดอร์ [{split.upper()}]:\")\n        print(f\"   - จำนวนภาพทั้งหมด: {image_count} ภาพ\")\n        print(f\"   - จำนวนภาพที่ 'ไม่มี Label' (Background): {unlabeled_count} ภาพ\\n\")\n        \n        total_images_all += image_count\n        total_unlabeled_all += unlabeled_count\n\n    print(\"-\" * 40)\n    print(\"📊 สรุปผลรวมทั้ง 3 โฟลเดอร์:\")\n    print(f\"   - ภาพทั้งหมด: {total_images_all} ภาพ\")\n    print(f\"   - ภาพที่ไม่มี Label ทั้งหมด: {total_unlabeled_all} ภาพ\")\n    print(\"-\" * 40)\n\n# วิธีใช้งาน: เปลี่ยน 'path/to/your/dataset' เป็นที่อยู่โฟลเดอร์ dataset ของคุณ\n# ตัวอย่าง: dataset_path = './my_yolo_dataset'\ndataset_path = '//kaggle/working/9dataset-v9-poly'\ncount_background_images(dataset_path)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-23T09:26:12.173811Z","iopub.execute_input":"2026-08-23T09:26:12.174744Z","iopub.status.idle":"2026-08-23T09:26:12.244205Z","shell.execute_reply.started":"2026-08-23T09:26:12.174708Z","shell.execute_reply":"2026-08-23T09:26:12.243202Z"}},"outputs":[{"name":"stdout","text":"--- เริ่มตรวจสอบ Dataset ที่: //kaggle/working/9dataset-v9-poly ---\n\n📁 โฟลเดอร์ [TRAIN]:\n   - จำนวนภาพทั้งหมด: 1702 ภาพ\n   - จำนวนภาพที่ 'ไม่มี Label' (Background): 150 ภาพ\n\n📁 โฟลเดอร์ [VALID]:\n   - จำนวนภาพทั้งหมด: 300 ภาพ\n   - จำนวนภาพที่ 'ไม่มี Label' (Background): 50 ภาพ\n\n📁 โฟลเดอร์ [TEST]:\n   - จำนวนภาพทั้งหมด: 183 ภาพ\n   - จำนวนภาพที่ 'ไม่มี Label' (Background): 30 ภาพ\n\n----------------------------------------\n📊 สรุปผลรวมทั้ง 3 โฟลเดอร์:\n   - ภาพทั้งหมด: 2185 ภาพ\n   - ภาพที่ไม่มี Label ทั้งหมด: 230 ภาพ\n----------------------------------------\n","output_type":"stream"}],"execution_count":8},{"cell_type":"code","source":"!pip install ultralytics","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-13T07:19:12.069767Z","iopub.execute_input":"2026-08-13T07:19:12.070201Z","iopub.status.idle":"2026-08-13T07:19:19.393835Z","shell.execute_reply.started":"2026-08-13T07:19:12.070162Z","shell.execute_reply":"2026-08-13T07:19:19.392842Z"}},"outputs":[{"name":"stdout","text":"Collecting ultralytics\n  Downloading ultralytics-8.4.118-py3-none-any.whl.metadata (45 kB)\n\u001b[2K     \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m45.5/45.5 kB\u001b[0m \u001b[31m1.9 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0m\n\u001b[?25hRequirement already satisfied: filelock>=3.16.1 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (3.20.3)\nRequirement already satisfied: numpy>=1.23.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (2.0.2)\nRequirement already satisfied: matplotlib>=3.3.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (3.10.0)\nRequirement already satisfied: opencv-python!=4.13.0.90,>=4.7.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (4.12.0.88)\nRequirement already satisfied: pillow>=7.1.2 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (11.3.0)\nRequirement already satisfied: pyyaml>=5.3.1 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (6.0.3)\nRequirement already satisfied: requests>=2.23.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (2.32.5)\nRequirement already satisfied: torch>=1.8.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (2.8.0+cu126)\nRequirement already satisfied: torchvision>=0.9.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (0.23.0+cu126)\nRequirement already satisfied: psutil>=5.8.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (5.9.5)\nRequirement already satisfied: polars>=0.20.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (1.25.2)\nRequirement already satisfied: nvidia-ml-py>=12.0.0 in /usr/local/lib/python3.12/dist-packages (from ultralytics) (12.575.51)\nCollecting ultralytics-thop>=2.1.6 (from ultralytics)\n  Downloading ultralytics_thop-2.1.6-py3-none-any.whl.metadata (13 kB)\nRequirement already satisfied: contourpy>=1.0.1 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (1.3.3)\nRequirement already satisfied: cycler>=0.10 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (0.12.1)\nRequirement already satisfied: fonttools>=4.22.0 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (4.60.1)\nRequirement already satisfied: kiwisolver>=1.3.1 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (1.4.9)\nRequirement already satisfied: packaging>=20.0 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (26.0rc2)\nRequirement already satisfied: pyparsing>=2.3.1 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (3.2.5)\nRequirement already satisfied: python-dateutil>=2.7 in /usr/local/lib/python3.12/dist-packages (from matplotlib>=3.3.0->ultralytics) (2.9.0.post0)\nRequirement already satisfied: charset_normalizer<4,>=2 in /usr/local/lib/python3.12/dist-packages (from requests>=2.23.0->ultralytics) (3.4.4)\nRequirement already satisfied: idna<4,>=2.5 in /usr/local/lib/python3.12/dist-packages (from requests>=2.23.0->ultralytics) (3.11)\nRequirement already satisfied: urllib3<3,>=1.21.1 in /usr/local/lib/python3.12/dist-packages (from requests>=2.23.0->ultralytics) (2.6.3)\nRequirement already satisfied: certifi>=2017.4.17 in /usr/local/lib/python3.12/dist-packages (from requests>=2.23.0->ultralytics) (2026.1.4)\nRequirement already satisfied: typing-extensions>=4.10.0 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (4.15.0)\nRequirement already satisfied: setuptools in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (75.2.0)\nRequirement already satisfied: sympy>=1.13.3 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (1.13.3)\nRequirement already satisfied: networkx in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (3.5)\nRequirement already satisfied: jinja2 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (3.1.6)\nRequirement already satisfied: fsspec in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (2025.10.0)\nRequirement already satisfied: nvidia-cuda-nvrtc-cu12==12.6.77 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.77)\nRequirement already satisfied: nvidia-cuda-runtime-cu12==12.6.77 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.77)\nRequirement already satisfied: nvidia-cuda-cupti-cu12==12.6.80 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.80)\nRequirement already satisfied: nvidia-cudnn-cu12==9.10.2.21 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (9.10.2.21)\nRequirement already satisfied: nvidia-cublas-cu12==12.6.4.1 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.4.1)\nRequirement already satisfied: nvidia-cufft-cu12==11.3.0.4 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (11.3.0.4)\nRequirement already satisfied: nvidia-curand-cu12==10.3.7.77 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (10.3.7.77)\nRequirement already satisfied: nvidia-cusolver-cu12==11.7.1.2 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (11.7.1.2)\nRequirement already satisfied: nvidia-cusparse-cu12==12.5.4.2 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.5.4.2)\nRequirement already satisfied: nvidia-cusparselt-cu12==0.7.1 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (0.7.1)\nRequirement already satisfied: nvidia-nccl-cu12==2.27.3 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (2.27.3)\nRequirement already satisfied: nvidia-nvtx-cu12==12.6.77 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.77)\nRequirement already satisfied: nvidia-nvjitlink-cu12==12.6.85 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (12.6.85)\nRequirement already satisfied: nvidia-cufile-cu12==1.11.1.6 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (1.11.1.6)\nRequirement already satisfied: triton==3.4.0 in /usr/local/lib/python3.12/dist-packages (from torch>=1.8.0->ultralytics) (3.4.0)\nRequirement already satisfied: six>=1.5 in /usr/local/lib/python3.12/dist-packages (from python-dateutil>=2.7->matplotlib>=3.3.0->ultralytics) (1.17.0)\nRequirement already satisfied: mpmath<1.4,>=1.1.0 in /usr/local/lib/python3.12/dist-packages (from sympy>=1.13.3->torch>=1.8.0->ultralytics) (1.3.0)\nRequirement already satisfied: MarkupSafe>=2.0 in /usr/local/lib/python3.12/dist-packages (from jinja2->torch>=1.8.0->ultralytics) (3.0.3)\nDownloading ultralytics-8.4.118-py3-none-any.whl (1.4 MB)\n\u001b[2K   \u001b[90m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\u001b[0m \u001b[32m1.4/1.4 MB\u001b[0m \u001b[31m21.3 MB/s\u001b[0m eta \u001b[36m0:00:00\u001b[0ma \u001b[36m0:00:01\u001b[0m\n\u001b[?25hDownloading ultralytics_thop-2.1.6-py3-none-any.whl (30 kB)\nInstalling collected packages: ultralytics-thop, ultralytics\nSuccessfully installed ultralytics-8.4.118 ultralytics-thop-2.1.6\n","output_type":"stream"}],"execution_count":1},{"cell_type":"code","source":"import os\nimport random\nimport shutil\nfrom pathlib import Path\n\ndef setup_and_add_backgrounds():\n    # ==========================================\n    # 1. กำหนด Path ต่างๆ\n    # ==========================================\n    ORIGINAL_DATASET = \"/kaggle/input/datasets/switchonchannel/9dataset-v8-bg\"\n    WORKING_DATASET = \"/kaggle/working/9dataset-v9-BG\"\n    BG_SOURCE = \"/kaggle/input/datasets/anshulm257/rice-disease-dataset/Rice_Leaf_AUG/Healthy Rice Leaf\"\n    \n    num_images = 80\n    split = \"train\"\n    PREFIX = \"bg_\"  # กำหนด Prefix ไว้ด้านบนเพื่อให้แก้ไขง่าย\n\n    # ==========================================\n    # 2. คัดลอก Dataset หลักมาที่ /kaggle/working/\n    # ==========================================\n    if not os.path.exists(WORKING_DATASET):\n        print(f\"⏳ [ขั้นตอนที่ 1] กำลังคัดลอก Dataset หลักมาที่: {WORKING_DATASET} ...\")\n        shutil.copytree(ORIGINAL_DATASET, WORKING_DATASET)\n        print(\"✅ คัดลอก Dataset หลักเสร็จสิ้น!\\n\")\n    else:\n        print(f\"✅ [ขั้นตอนที่ 1] พบ Dataset ใน {WORKING_DATASET} แล้ว ไม่ต้องคัดลอกใหม่\\n\")\n\n    # ==========================================\n    # 3. เริ่มกระบวนการเพิ่มภาพ Background\n    # ==========================================\n    print(\"⏳ [ขั้นตอนที่ 2] กำลังเตรียมสุ่มภาพ Background...\")\n    source_dir = Path(BG_SOURCE)\n    dest_img_dir = Path(WORKING_DATASET) / split / \"images\"\n    dest_lbl_dir = Path(WORKING_DATASET) / split / \"labels\"\n\n    if not source_dir.exists():\n        print(f\"❌ ไม่พบโฟลเดอร์ Background ต้นทาง: {source_dir}\")\n        return\n\n    # หาไฟล์ภาพทั้งหมด\n    valid_extensions = {'.jpg', '.jpeg', '.png', '.bmp', '.webp'}\n    all_images = [f for f in source_dir.iterdir() if f.is_file() and f.suffix.lower() in valid_extensions]\n\n    if len(all_images) < num_images:\n        num_images = len(all_images)\n\n    selected_images = random.sample(all_images, num_images)\n\n    print(f\"--- ⏳ กำลังคัดลอกภาพ Background จำนวน {num_images} ภาพ เข้าสู่ชุด {split.upper()} ---\")\n    \n    success_count = 0\n    for img_path in selected_images:\n        # ประกอบชื่อไฟล์ใหม่โดยใส่ Prefix เข้าไปข้างหน้า\n        stem = PREFIX + img_path.stem\n        new_img_path = dest_img_dir / f\"{stem}{img_path.suffix.lower()}\"\n        new_lbl_path = dest_lbl_dir / f\"{stem}.txt\"\n\n        # กันทับไฟล์เดิมแบบเงียบๆ\n        if new_img_path.exists() or new_lbl_path.exists():\n            print(f\"⚠️ ข้าม (ชื่อซ้ำ): {new_img_path.name}\")\n            continue\n\n        try:\n            # คัดลอกรูปภาพพร้อมชื่อใหม่\n            shutil.copy2(img_path, new_img_path)\n            \n            # สร้างไฟล์ label เป็นไฟล์ว่างเปล่าด้วยชื่อใหม่\n            new_lbl_path.touch()\n            \n            success_count += 1\n        except Exception as e:\n            print(f\"❌ เกิดข้อผิดพลาดกับไฟล์ {img_path.name}: {e}\")\n\n    print(\"-\" * 50)\n    print(f\"✅ สำเร็จ! เพิ่มภาพและสร้าง Label ว่างเปล่า: {success_count}/{num_images} ภาพ\")\n    print(f\"📁 Dataset ที่พร้อมใช้งาน (ใช้โฟลเดอร์นี้เทรนโมเดล): {WORKING_DATASET}\")\n    print(\"-\" * 50)\n\n# รันฟังก์ชัน\nsetup_and_add_backgrounds()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-08-23T09:29:18.641334Z","iopub.execute_input":"2026-08-23T09:29:18.641646Z","iopub.status.idle":"2026-08-23T09:29:56.24698Z","shell.execute_reply.started":"2026-08-23T09:29:18.641613Z","shell.execute_reply":"2026-08-23T09:29:56.24597Z"}},"outputs":[{"name":"stdout","text":"⏳ [ขั้นตอนที่ 1] กำลังคัดลอก Dataset หลักมาที่: /kaggle/working/9dataset-v9-BG ...\n✅ คัดลอก Dataset หลักเสร็จสิ้น!\n\n⏳ [ขั้นตอนที่ 2] กำลังเตรียมสุ่มภาพ Background...\n--- ⏳ กำลังคัดลอกภาพ Background จำนวน 80 ภาพ เข้าสู่ชุด TRAIN ---\n--------------------------------------------------\n✅ สำเร็จ! เพิ่มภาพและสร้าง Label ว่างเปล่า: 80/80 ภาพ\n📁 Dataset ที่พร้อมใช้งาน (ใช้โฟลเดอร์นี้เทรนโมเดล): /kaggle/working/9dataset-v9-BG\n--------------------------------------------------\n","output_type":"stream"}],"execution_count":11},{"cell_type":"code","source":"import ipywidgets as widgets\nfrom IPython.display import display\nfrom PIL import Image\nfrom ultralytics import YOLO\nimport io\n\n# 1. โหลดโมเดล (ใช้ Path เดิมของคุณ)\nmodel_path = '/kaggle/input/models/switchonchannel/9data-model-v6-bg/tensorflow2/default/1/weights/best.pt'\ntry:\n    model = YOLO(model_path)\nexcept Exception as e:\n    print(f\"⚠️ หาโมเดลไม่เจอ หรือ Path ผิด: {e}\")\n    # ถ้าหาไม่เจอ ให้ลองใช้โมเดลมาตรฐานทดสอบไปก่อน\n    # model = YOLO('yolo11n.pt') \n\n# 2. สร้างปุ่ม Upload\nuploader = widgets.FileUpload(\n    accept='image/*',   # รับเฉพาะไฟล์รูปภาพ\n    multiple=False,     # รับทีละ 1 รูป\n    description='Upload/Drag Image'\n)\n\n# 3. พื้นที่แสดงผลลัพธ์\noutput_area = widgets.Output()\n\nprint(\"⬇️ ลากไฟล์รูปมาวางที่ปุ่มด้านล่าง หรือกดคลิกเพื่อเลือกรูป:\")\ndisplay(uploader, output_area)\n\n# 4. ฟังก์ชันทำงานเมื่อมีการใส่รูปเข้ามา\ndef on_upload_change(change):\n    if not uploader.value:\n        return\n        \n    with output_area:\n        output_area.clear_output() # ล้างผลเก่า\n        print(\"⏳ กำลังประมวลผล...\")\n        \n        try:\n            # ดึงข้อมูลไฟล์ (รองรับ ipywidgets เวอร์ชั่นใหม่)\n            # uploader.value จะเป็น Tuple ของ Dict\n            latest_file = uploader.value[0] \n            content = latest_file['content']\n            filename = latest_file['name']\n            \n            # แปลงไบนารีเป็นรูปภาพเพื่อส่งเข้าโมเดล (ไม่ต้องเซฟลง Disk ก็ได้)\n            image = Image.open(io.BytesIO(content))\n            \n            # สั่งทำนาย\n            results = model.predict(image, conf=0.25)\n            \n            # แสดงผล\n            for r in results:\n                print(f\"✅ ผลการทำนาย: {filename}\")\n                # แปลงผลลัพธ์ (Array BGR) เป็นรูป (RGB) เพื่อแสดงผล\n                im_array = r.plot() \n                im_rgb = Image.fromarray(im_array[..., ::-1]) \n                display(im_rgb)\n                \n        except Exception as e:\n            print(f\"❌ เกิดข้อผิดพลาด: {e}\")\n            # กรณีเวอร์ชั่น ipywidgets เก่า อาจต้องแก้บรรทัด latest_file\n            print(\"ลองเช็ครูปแบบข้อมูล: \", type(uploader.value))\n\n# ผูกฟังก์ชันเข้ากับปุ่ม\nuploader.observe(on_upload_change, names='value')","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}